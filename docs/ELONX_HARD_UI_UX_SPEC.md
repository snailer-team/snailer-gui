# ElonX HARD Mode — UI/UX 계획서

> Snailer GUI를 "코딩 도구"가 아니라 **AI 소프트웨어 회사 운영체제**로 만들기 위한 UI/UX 설계

---

## 1. 설계 원칙

### 1.1 핵심 철학
- **Human-0**: 기본적으로 인간 개입 없이 동작 (관찰만 가능)
- **Evidence-first**: 모든 결정은 근거(테스트, 로그, 스크린샷)로 뒷받침
- **Short & Sharp**: 텍스트 최소화, 수치/상태 중심 표현

### 1.2 UI 톤
| 원칙 | 적용 |
|------|------|
| 텍스트 최소 | 라벨은 1-3 단어, 설명은 한 줄 이내 |
| 수치 중심 | 시간(ms/s), 성공률(%), 변경량(LOC) |
| 근거 중심 | 모든 완료 항목에 검증 결과 배지 |
| 색상 의미 | 상태별 일관된 색상 시스템 사용 |

---

## 2. 전체 레이아웃

### 2.1 ElonX HARD 모드 진입 시 화면 구조

```
┌─────────────────────────────────────────────────────────────────┐
│ [Header] Goal Bar + Intervene Controls                          │
├──────────────────┬──────────────────────┬───────────────────────┤
│                  │                      │                       │
│   Org Panel      │    Plan Tree         │   Evidence Panel      │
│   (에이전트 조직)   │    (작업 트리)         │   (증거/검증)           │
│                  │                      │                       │
│   260px          │    flex-1            │   320px               │
│                  │                      │                       │
├──────────────────┴──────────────────────┴───────────────────────┤
│ [Footer] Status Bar: Cycle Time | Autonomy Rate | Cost          │
└─────────────────────────────────────────────────────────────────┘
```

### 2.2 반응형 동작
- **Desktop (≥1280px)**: 3컬럼 레이아웃
- **Tablet (768-1279px)**: 2컬럼 (Org+Plan | Evidence)
- **Mobile (<768px)**: 탭 전환 방식

---

## 3. 핵심 UI 컴포넌트

### 3.1 Goal Bar (목표 입력)

**위치**: 상단 고정

**구조**:
```
┌────────────────────────────────────────────────────────────────┐
│ [Problem] _____________________                                │
│ [Constraints] _______________ [Verification] _______________   │
│                                            [▶ Execute] [⏸]     │
└────────────────────────────────────────────────────────────────┘
```

**상태**:
- `idle`: 입력 가능
- `running`: 읽기 전용 + 진행 표시
- `paused`: Resume 버튼 활성화

**컴포넌트**: `<GoalBar />`
```tsx
type GoalBarProps = {
  problem: string
  constraints: string
  verification: string
  status: 'idle' | 'running' | 'paused' | 'completed'
  onExecute: () => void
  onPause: () => void
  onResume: () => void
}
```

---

### 3.2 Org Panel (에이전트 조직도)

**위치**: 좌측 사이드바 (260px)

**현재 구현됨**: `ElonOrgPanel.tsx`

**개선 사항**:

#### 3.2.1 실시간 상태 연결
```tsx
type AgentStatus = 'idle' | 'observing' | 'planning' | 'acting' | 'evaluating' | 'blocked'

// 각 에이전트 카드에 표시
<AgentCard
  agent={agent}
  status={agentStatus[agent.id]}  // 실시간 상태
  currentTask={currentTasks[agent.id]}  // 현재 수행 중인 작업
  lastOutput={lastOutputs[agent.id]}  // 마지막 산출물 요약
/>
```

#### 3.2.2 상태 배지 시스템
| 상태 | 색상 | 아이콘 |
|------|------|--------|
| idle | gray/50 | ○ |
| observing | blue/60 | 👁 |
| planning | amber/60 | 📋 |
| acting | green/60 | ▶ |
| evaluating | violet/60 | ✓ |
| blocked | red/60 | ⚠ |

#### 3.2.3 축약 뷰 (기본)
```
┌─────────────────────────────┐
│ Leadership                  │
│  ├ CEO      [▶ acting]      │
│  ├ CTO      [○ idle]        │
│  └ CPO      [📋 planning]   │
│ Engineering                 │
│  ├ SWE      [▶ acting]      │
│  └ SRE      [○ idle]        │
└─────────────────────────────┘
```

#### 3.2.4 확장 뷰 (클릭 시)
- 현재 작업 내용
- ReAct 루프 단계
- 최근 산출물 링크
- 소유 중인 파일/리소스

---

### 3.3 Plan Tree (작업 트리) — **신규 구현 필요**

**위치**: 중앙 메인 영역

**목적**: 현재 무엇이 병목인지, 다음 액션이 무엇인지 한눈에 파악

**컴포넌트**: `<PlanTree />`

#### 3.3.1 데이터 구조
```tsx
type PlanNode = {
  id: string
  title: string  // 짧게: "Implement auth API"
  status: 'pending' | 'running' | 'blocked' | 'completed' | 'failed'
  assignee: string  // agent id (예: 'swe', 'pm')
  children: PlanNode[]

  // 메트릭
  estimatedMs?: number
  actualMs?: number
  blockedBy?: string[]  // 다른 노드 ID

  // 검증
  verification?: {
    type: 'test' | 'lint' | 'build' | 'manual'
    result?: 'pass' | 'fail' | 'pending'
    evidence?: string  // Evidence Panel 링크
  }
}
```

#### 3.3.2 시각적 표현
```
┌─────────────────────────────────────────────────────────────┐
│ ▼ Goal: Add user authentication                             │
│   │                                                         │
│   ├─● Design auth flow          [PM]    ✓ 12s              │
│   │   └─○ Review by CTO         [CTO]   ✓ 5s               │
│   │                                                         │
│   ├─● Implement API             [SWE]   ▶ 45s...           │
│   │   ├─○ /login endpoint              ✓                   │
│   │   ├─○ /logout endpoint             ▶ running           │
│   │   └─○ JWT middleware               ○ pending           │
│   │                                                         │
│   ├─○ Write tests               [SWE]   ○ blocked          │
│   │   └ ⚠ Waiting: Implement API                           │
│   │                                                         │
│   └─○ Deploy to staging         [SRE]   ○ pending          │
│                                                             │
│ ─────────────────────────────────────────────────────────── │
│ Bottleneck: Implement API → /logout endpoint                │
│ ETA: ~2m remaining                                          │
└─────────────────────────────────────────────────────────────┘
```

#### 3.3.3 노드 상태 스타일
| 상태 | 기호 | 배경 | 테두리 |
|------|------|------|--------|
| pending | ○ | white | black/10 |
| running | ▶ | blue/5 | blue/20, pulse |
| blocked | ⚠ | amber/5 | amber/20 |
| completed | ✓ | green/5 | green/20 |
| failed | ✗ | red/5 | red/20 |

#### 3.3.4 인터랙션
- **클릭**: 노드 상세 정보 (Evidence Panel에 표시)
- **우클릭**: Intervene 메뉴 (Retry, Skip, Split, Reassign)
- **드래그**: 우선순위 재조정 (Owner만)
- **키보드**: ↑↓ 탐색, Enter 확장/축소, R 재시도

---

### 3.4 Evidence Panel (증거 패널) — **신규 구현 필요**

**위치**: 우측 사이드바 (320px)

**목적**: 모든 결정/완료의 근거를 검증 가능한 형태로 표시

**컴포넌트**: `<EvidencePanel />`

#### 3.4.1 증거 타입
```tsx
type EvidenceType =
  | 'test_result'    // 테스트 결과
  | 'build_log'      // 빌드 로그
  | 'lint_report'    // 린트 리포트
  | 'screenshot'     // VM 스크린샷
  | 'diff'           // 코드 변경
  | 'terminal'       // 터미널 출력
  | 'api_response'   // API 응답
  | 'metric'         // 성능/비용 메트릭

type Evidence = {
  id: string
  type: EvidenceType
  title: string
  timestamp: number
  relatedNodeId: string  // Plan Tree 노드 연결
  relatedAgentId: string

  // 타입별 데이터
  data: TestResult | BuildLog | Screenshot | Diff | ...

  // 요약
  summary: string  // 한 줄 요약
  verdict: 'pass' | 'fail' | 'warning' | 'info'
}
```

#### 3.4.2 레이아웃
```
┌─────────────────────────────────┐
│ Evidence                    [🔍]│
├─────────────────────────────────┤
│ ┌─────────────────────────────┐ │
│ │ ✓ Test: auth.test.ts       │ │
│ │   5/5 passed · 120ms       │ │
│ │   [SWE] · 2s ago           │ │
│ └─────────────────────────────┘ │
│ ┌─────────────────────────────┐ │
│ │ ✓ Build: success           │ │
│ │   0 errors, 2 warnings     │ │
│ │   [SRE] · 15s ago          │ │
│ └─────────────────────────────┘ │
│ ┌─────────────────────────────┐ │
│ │ 📷 Screenshot: login page  │ │
│ │   VM verification passed   │ │
│ │   [UX] · 30s ago           │ │
│ │   [View full]              │ │
│ └─────────────────────────────┘ │
│ ┌─────────────────────────────┐ │
│ │ 📝 Diff: src/auth/login.ts │ │
│ │   +45 -12 lines            │ │
│ │   [SWE] · 1m ago           │ │
│ └─────────────────────────────┘ │
├─────────────────────────────────┤
│ Filter: [All ▼] [Pass ○] [Fail]│
└─────────────────────────────────┘
```

#### 3.4.3 상세 뷰 (클릭 시)
- **Test Result**: 각 테스트 케이스 결과, 실패 시 에러 메시지
- **Build Log**: 전체 로그 (접기/펼치기), 에러 위치 하이라이트
- **Screenshot**: 전체 이미지, 확대/축소, 영역 비교
- **Diff**: 기존 DiffViewer 컴포넌트 재사용

---

### 3.5 Intervene Controls (최소 개입)

**위치**: Goal Bar 우측 + Plan Tree 컨텍스트 메뉴

**원칙**: 고레버리지 액션만 제공, 세부 제어는 최소화

#### 3.5.1 글로벌 컨트롤 (Goal Bar)
| 액션 | 아이콘 | 단축키 | 설명 |
|------|--------|--------|------|
| Pause | ⏸ | `Cmd+P` | 전체 실행 일시정지 |
| Resume | ▶ | `Cmd+R` | 실행 재개 |
| Abort | ⏹ | `Cmd+.` | 전체 중단 + 롤백 옵션 |

#### 3.5.2 노드별 컨트롤 (Plan Tree 우클릭)
| 액션 | 설명 |
|------|------|
| Retry | 실패한 작업 재시도 |
| Skip | 건너뛰기 (의존성 무시) |
| Split | 작업을 하위 작업으로 분할 |
| Reassign | 다른 에이전트에게 재할당 |
| Approve | 고위험 액션 승인 |

#### 3.5.3 승인 게이트 (자동 팝업)
```
┌─────────────────────────────────────────┐
│ ⚠ Approval Required                     │
├─────────────────────────────────────────┤
│ Agent: SRE                              │
│ Action: Deploy to production            │
│ Risk Level: HIGH                        │
│                                         │
│ Evidence:                               │
│  ✓ All tests passed                     │
│  ✓ Staging verification OK              │
│  ⚠ 3 new dependencies                   │
│                                         │
│ [Approve]  [Reject]  [Request Change]   │
│                                         │
│ Auto-reject in: 5:00                    │
└─────────────────────────────────────────┘
```

---

### 3.6 Status Bar (하단 상태바)

**위치**: 하단 고정

**표시 항목**:
```
┌────────────────────────────────────────────────────────────────┐
│ Cycle: 2m 34s | Autonomy: 94% | Tasks: 8/12 | Cost: $0.42     │
│ ▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓░░░░░░░░ 67%                                  │
└────────────────────────────────────────────────────────────────┘
```

| 메트릭 | 설명 | 업데이트 |
|--------|------|----------|
| Cycle Time | Goal 시작 후 경과 시간 | 1초마다 |
| Autonomy Rate | 인간 개입 없이 완료된 비율 | 작업 완료 시 |
| Tasks | 완료/전체 작업 수 | 상태 변경 시 |
| Cost | 예상 토큰 비용 | API 호출 시 |
| Progress | 전체 진행률 바 | 상태 변경 시 |

---

## 4. 상태 관리

### 4.1 Store 확장 (`store.ts`)

```tsx
// ElonX HARD 전용 상태
interface ElonXState {
  // 목표
  currentGoal: {
    problem: string
    constraints: string
    verification: string
    status: 'idle' | 'running' | 'paused' | 'completed'
    startedAt?: number
  }

  // Plan Tree
  planTree: PlanNode | null
  selectedNodeId: string | null

  // 에이전트 상태
  agentStatuses: Record<string, AgentStatus>
  agentCurrentTasks: Record<string, string>  // agent.id -> task title

  // Evidence
  evidences: Evidence[]
  selectedEvidenceId: string | null

  // 메트릭
  metrics: {
    cycleStartMs: number
    autonomyRate: number
    completedTasks: number
    totalTasks: number
    estimatedCost: number
  }

  // 승인 대기
  pendingApprovals: ApprovalRequest[]
}

// 액션
interface ElonXActions {
  // 실행 제어
  executeGoal: () => Promise<void>
  pauseExecution: () => void
  resumeExecution: () => void
  abortExecution: () => void

  // Plan Tree
  selectNode: (nodeId: string) => void
  retryNode: (nodeId: string) => void
  skipNode: (nodeId: string) => void

  // 승인
  approveAction: (approvalId: string) => void
  rejectAction: (approvalId: string, reason?: string) => void
}
```

### 4.2 Daemon 이벤트 매핑

```tsx
// daemon → UI 이벤트
type DaemonEvent =
  | { type: 'plan_update'; tree: PlanNode }
  | { type: 'agent_status'; agentId: string; status: AgentStatus; task?: string }
  | { type: 'evidence_add'; evidence: Evidence }
  | { type: 'approval_request'; request: ApprovalRequest }
  | { type: 'metrics_update'; metrics: Partial<ElonXState['metrics']> }
  | { type: 'goal_status'; status: ElonXState['currentGoal']['status'] }
```

---

## 5. 구현 로드맵

### Phase 1: 기반 작업 (MVP-1 준비)

#### Week 1-2
| 작업 | 파일 | 우선순위 |
|------|------|----------|
| ElonX Store 확장 | `lib/store.ts` | P0 |
| PlanNode 타입 정의 | `lib/elonPlan.ts` | P0 |
| Evidence 타입 정의 | `lib/elonEvidence.ts` | P0 |
| Goal Bar 컴포넌트 | `components/ElonGoalBar.tsx` | P0 |

#### Week 3-4
| 작업 | 파일 | 우선순위 |
|------|------|----------|
| Plan Tree 기본 UI | `components/ElonPlanTree.tsx` | P0 |
| Evidence Panel 기본 UI | `components/ElonEvidencePanel.tsx` | P0 |
| Status Bar | `components/ElonStatusBar.tsx` | P1 |
| Org Panel 상태 연결 | `components/ElonOrgPanel.tsx` | P1 |

### Phase 2: 실행 연결 (MVP-2 준비)

#### Week 5-6
| 작업 | 파일 | 우선순위 |
|------|------|----------|
| Daemon RPC 스키마 정의 | `lib/elonRpc.ts` | P0 |
| Plan Tree ↔ Daemon 연결 | `components/ElonPlanTree.tsx` | P0 |
| Evidence 실시간 수집 | `lib/elonEvidence.ts` | P0 |

#### Week 7-8
| 작업 | 파일 | 우선순위 |
|------|------|----------|
| Intervene 컨트롤 구현 | `components/ElonIntervene.tsx` | P0 |
| 승인 게이트 UI | `components/ElonApprovalGate.tsx` | P0 |
| 에이전트 실시간 상태 | `components/ElonOrgPanel.tsx` | P1 |

### Phase 3: 검증 루프 (MVP-3 준비)

#### Week 9-10
| 작업 | 파일 | 우선순위 |
|------|------|----------|
| VM 스크린샷 증거 | `lib/elonEvidence.ts` | P1 |
| 테스트 결과 파싱 | `lib/elonTestParser.ts` | P1 |
| Plan Repair UI | `components/ElonPlanTree.tsx` | P2 |

#### Week 11-12
| 작업 | 파일 | 우선순위 |
|------|------|----------|
| 메트릭 대시보드 | `components/ElonMetrics.tsx` | P2 |
| 비용 추적/제한 | `lib/elonCost.ts` | P2 |
| 감사 로그 UI | `components/ElonAuditLog.tsx` | P2 |

---

## 6. 파일 구조

```
src/
├── components/
│   ├── elon/
│   │   ├── ElonLayout.tsx        # 3컬럼 레이아웃 래퍼
│   │   ├── ElonGoalBar.tsx       # 목표 입력 바
│   │   ├── ElonOrgPanel.tsx      # 에이전트 조직 (기존 이동+개선)
│   │   ├── ElonPlanTree.tsx      # 작업 트리
│   │   ├── ElonPlanNode.tsx      # 트리 노드 컴포넌트
│   │   ├── ElonEvidencePanel.tsx # 증거 패널
│   │   ├── ElonEvidenceCard.tsx  # 증거 카드 컴포넌트
│   │   ├── ElonStatusBar.tsx     # 하단 상태바
│   │   ├── ElonIntervene.tsx     # 개입 컨트롤
│   │   ├── ElonApprovalGate.tsx  # 승인 모달
│   │   └── ElonMetrics.tsx       # 메트릭 대시보드
│   └── ...
├── lib/
│   ├── elonOrg.ts                # 에이전트 정의 (기존)
│   ├── elonPlan.ts               # Plan Tree 타입/유틸
│   ├── elonEvidence.ts           # Evidence 타입/유틸
│   ├── elonRpc.ts                # Daemon RPC 스키마
│   ├── elonCost.ts               # 비용 추적
│   └── modes.ts                  # 모드 관리 (기존)
└── ...
```

---

## 7. 디자인 토큰

### 7.1 색상 시스템

```css
/* 상태 색상 */
--elon-idle: #9ca3af;       /* gray-400 */
--elon-observing: #3b82f6;  /* blue-500 */
--elon-planning: #f59e0b;   /* amber-500 */
--elon-acting: #22c55e;     /* green-500 */
--elon-evaluating: #8b5cf6; /* violet-500 */
--elon-blocked: #ef4444;    /* red-500 */
--elon-completed: #10b981;  /* emerald-500 */

/* 검증 결과 */
--elon-pass: #22c55e;
--elon-fail: #ef4444;
--elon-warning: #f59e0b;
--elon-info: #3b82f6;
```

### 7.2 타이포그래피

```css
/* ElonX HARD는 모노스페이스 기조 */
--font-elon-mono: 'JetBrains Mono', 'SF Mono', monospace;

/* 크기 */
--text-elon-xs: 10px;   /* 배지, 타임스탬프 */
--text-elon-sm: 12px;   /* 보조 텍스트 */
--text-elon-base: 14px; /* 기본 */
--text-elon-lg: 16px;   /* 제목 */
```

### 7.3 간격

```css
/* 컴팩트 간격 (정보 밀도 높게) */
--space-elon-1: 4px;
--space-elon-2: 8px;
--space-elon-3: 12px;
--space-elon-4: 16px;
```

---

## 8. 접근성

- **키보드 탐색**: 모든 인터랙티브 요소에 Tab 접근 가능
- **스크린 리더**: 상태 변경 시 aria-live 알림
- **색상 대비**: WCAG AA 기준 충족 (4.5:1 이상)
- **모션**: `prefers-reduced-motion` 존중

---

## 9. 다음 단계

1. **이 문서 리뷰** → 피드백 반영
2. **Daemon RPC 스키마 정의** → 백엔드 팀과 협의
3. **Phase 1 구현 착수** → ElonX Store + Goal Bar + Plan Tree 기본

---

*Last updated: 2026-01-29*
*Author: Claude (Snailer GUI)*
